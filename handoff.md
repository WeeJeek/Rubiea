# Project Handoff

## 2026-07-27

- **What changed:** Recorded the confirmed Rubiae sales and brand agreements, domain language, supplier questionnaire, project TODO index, and detailed unimplemented work.
- **Relevant commits:** `6a84e75` (initial agreement documentation), `5e9caa7` (confirmed Rubiae brand spelling)
- **Verification:** Confirmed all eight `GEM-001`–`GEM-008` IDs appear in both active-work files; verified documentation link targets exist; scanned for placeholders and contradictory agreement/open-decision wording; confirmed no old `Rubiea` spelling remains in project files; confirmed a clean worktree after the documentation commit.
- **Open IDs:** `GEM-001`, `GEM-002`, `GEM-003`, `GEM-004`, `GEM-005`, `GEM-006`, `GEM-007`, `GEM-008`.
- **Next planned work:** User reviews the written agreement, then supplier P0 questions are resolved before the next shipment or buyer quotation.
- **Automation audit evidence (2026-07-28):** Git recorded four 2026-07-27 documentation commits: `6a84e75` (`docs: capture Rubiea sales agreements`, 22:42), `bf0fa23` (`docs: add project handoff`, 22:42), `5e9caa7` (`docs: confirm Rubiae brand spelling`, 23:59), and `e79cff5` (`docs: update handoff for brand decision`, 23:59), Europe/Amsterdam. The worktree was clean in two checks before this automation updated `handoff.md`, and `git diff --check` passed. Codex thread-context query did not return within 50 seconds, so **context evidence unavailable**; agent status showed only this automation, with no project worker. No separate test, build, or browser-verification execution was evidenced; no related day-activity file changes remained uncommitted before this handoff update. Open IDs and next work remain as above.

## 2026-07-28

- **What changed:** Moved the business design document out of the skill-specific `superpowers` directory and renamed it [Rubiae品牌经营基本信息](docs/rubiae-business-basics.md); updated all source links.
- **What changed:** Selected visual direction A (“静默纪念”) for Rubiae: warm white, deep gemstone red, restrained old gold, ink gray, natural warm light, and a hand choosing a loose stone as the primary visual moment. Updated the brand foundation, TODO, and unimplemented-feature scope.
- **Relevant commits:** `9e53152` (move Rubiae business basics), `ddcac8c` (select visual direction A)
- **Verification:** Confirmed the old path and `superpowers` references are gone, the new target exists, `git diff --check` passes, and `GEM-005` remains open for exact color values, fonts, components, and site/directory design.
- **Open IDs:** `GEM-001`, `GEM-002`, `GEM-003`, `GEM-004`, `GEM-005`, `GEM-006`, `GEM-007`, `GEM-008`.
- **Next planned work:** Resolve supplier P0 questions before the next shipment or buyer quotation.

## 2026-07-29

- **Project and canonical root:** Rubiae，`/Users/jiekewei/Documents/3 - Projects/6 - 红宝石`。
- **Branch / revision:** `main` at `e61114a`; this documentation work is not committed.
- **Completed:** Created [Rubiae 公开预览站内容源](docs/website/rubiae-preview-site-content.md) with EN/NL copy for Home, Stones, Stone Detail, Stories, How to Choose, About, For Trade, Contact, Privacy, Cookies, forms, system states, SEO, asset mapping, and preview acceptance criteria. Archived the user-confirmed design screenshot as `docs/brand/rubiae-homepage-approved-layout-reference.jpg`; retained `docs/brand/rubiae-homepage-still-life-concept-v5.png` as the higher-resolution implementation reference. Updated `GEM-005` in both active-work records.
- **Intentionally unstarted:** No Shopify theme, frontend project, page, component, or website code was created. Business-operation dependencies and paid commerce remain deferred.
- **Next visual decision already recorded:** Keep the approved layout, hero scene, and page order; replace loose ruby particles in website scenes with realistic, correctly scaled finished ruby jewellery. Every scene still contains ruby; preserve the approved brightness and cool berry-red hue. Product cards and galleries must use matching real-product photography.
- **Verification:** `git diff --check` passed. Confirmed the archived JPEG is readable at 524 × 1280, the primary v5 PNG remains available at 803 × 1959, all linked design/content files exist, and the content source contains 547 lines. Documentation-only step; no automated product tests or expected-red tests apply.
- **Uncommitted state:** Inspected with `git status --short`. Modified tracked files: `CONTEXT.md`, `docs/brand/rubiae-web-visual-spec.md`, `docs/project-todos.md`, `docs/rubiae-brand-foundation.md`, `docs/rubiae-business-basics.md`, and `docs/unimplemented-features.md`. New task files: `docs/website/rubiae-preview-site-content.md` and `docs/brand/rubiae-homepage-approved-layout-reference.jpg`. Unrelated untracked `.DS_Store`, v2–v4 concept PNGs, and `学习资料/` remain untouched.
- **Blockers and deferred data:** None for starting the public-preview build. Before public release, fill the legal name, domain, contact/privacy email, real stone facts and photography, authorized stories or the supplied empty state, actual cookie inventory, and final jewellery assets. KVK, VAT, payments, logistics, and other transaction gates remain under `GEM-008` and do not block preview-site construction.
- **Open IDs:** `GEM-001`, `GEM-002`, `GEM-003`, `GEM-004`, `GEM-005`, `GEM-006`, `GEM-007`, `GEM-008`, `GEM-009`, `GEM-010`.
- **Exact next action:** In the new window, read `docs/website/rubiae-preview-site-content.md` and `docs/brand/rubiae-web-visual-spec.md`, then build the Shopify public-preview Home page from `docs/brand/rubiae-homepage-still-life-concept-v5.png`, using jewellery rather than loose ruby particles.
- **Later implementation:** Built the Shopify preview-only theme through public editorial pages in commits `faf2c3a` through `a3d5c65`. Built an uncommitted local React visual prototype at `preview-prototype/` from the approved v5 composition, with six text-free ruby-jewellery scene images and no transaction controls.
- **Visual verification:** Rejected the first broken stitched preview, then iterated through independent subagent reviews. Final 1440 × 900 desktop and 390 × 844 mobile checks passed with all images loaded, no overlap or horizontal overflow, working EN/NL and mobile menu, synchronized `html.lang`, visible keyboard focus and skip link. The approved Hero revision restores the right-side Moments feature as a large paper sheet rotated `3deg` clockwise on desktop and unrotated in mobile flow. `npm run build`, `npm test` (1/1), and `npm run test:sites` (4/4) passed; no price, cart, checkout, payment, reservation, inventory, availability, product form, or Offer schema was found. Independent final review: PASS, no blockers.
- **Open status:** `GEM-005` remains open for real business/contact/privacy values, real inventory facts and photography, Shopify Admin template assignment, and public hosting. The user approved the local prototype and Hero letter revision for commit.
- **Next action after approval:** Keep all transaction functionality disabled. Replace demonstration facts and imagery with verified real content before any public hosting or Shopify Admin assignment.

## 2026-07-30

### Stories and Moment desktop design approval

- **Completed:** Approved `Stories` as a Letter Archive-style cover and directory that publishes from the first fully authorised Moment. Each Moment keeps an independent URL; book-page transitions occur only inside one customer's Moment and never across customers.
- **Completed:** Defined `顾客故事影像` and `Rubiae Moment 文稿`. Customers choose whether and how to provide story imagery; Rubiae may only make customer-approved technical image adjustments. Customer text may receive meaning-preserving light edits and customer-approved title or excerpt proposals.
- **Completed:** Preserved original-language text alongside reviewed EN/NL translations. AI may create a translation draft only after separate consent, followed by target-language human review; it may not auto-publish. When the customer cannot read the target language, ambiguous choices are explained in a language they understand.
- **Approved reference:** `docs/brand/rubiae-stories-moment-book-approved-reference.png`; review-only design evidence, not customer content or product evidence.
- **Design record:** `docs/superpowers/specs/2026-07-30-stories-moment-book-design.md`.
- **Verification:** Documentation and approved-reference step only; no production code or automated product tests. Checked document links, image readability and dimensions, placeholder semantics, decision consistency, and `git diff --check` before commit.
- **Open:** `GEM-009` remains open for mobile composition, no-image Moment layout, authorised Stone association card, permission workflow implementation, and accessibility or interaction testing.
- **Next action:** Define and approve the mobile Moment reading composition before any Stories or Moment implementation.

### Stories interaction direction revised

- **Supersedes:** The separate Letter Archive cover and internal page-turn interaction recorded above. The old reference remains historical design evidence and is no longer the implementation baseline.
- **Confirmed:** Stories now uses one directly readable desktop interface. A narrow left index uses customer-approved summary titles and one-line synopses to survey indexed non-anonymous Moments; selecting one synchronously replaces the right-hand authorised object or image, attribution, and complete story without a second read-more step.
- **Confirmed:** Every indexed non-anonymous Moment retains its independent URL within the shared interface. Switching does not autoplay and must support pointer, keyboard, touch, and reduced-motion use. The right-hand display is editorial composition, not evidence that Rubiae made or sells depicted jewellery.
- **Confirmed:** A Moment without customer imagery or an object keeps the plinth intentionally empty and uses the story paper as its sole exhibit. Rubiae does not add generic gemstone imagery, generated symbols, decorative objects, or substitute photography.
- **Confirmed:** Ordinary mobile Stories opens directly on the first or URL-selected indexed non-anonymous Moment. A current-title plus `All stories / Alle verhalen` control opens a bottom sheet for switching those stories; the sheet is not a reading gate, and selection closes it, updates the independent URL, and returns to the same single-column reading surface.
- **Confirmed:** Anonymous Moments do not appear in the desktop story index or mobile bottom sheet. They are discovered only through a separate visitor-initiated random entry; randomness is an encounter pattern, not privacy protection.
- **Confirmed:** Whenever at least one eligible anonymous Moment exists, the random entry is persistently visible as `Encounter an anonymous Moment / Ontmoet een anoniem moment`. Pointer, touch, and keyboard activation each select one eligible anonymous Moment; discovery does not depend on hover or a timed ambient event.
- **Confirmed:** The selected anonymous Moment replaces the existing right-hand desktop display or mobile single-column reading surface. It does not open in a modal or separate intermediate page. Ordinary directories remain unchanged, and `Back to stories / Terug naar verhalen` restores the previously visible indexed Moment.
- **Confirmed:** The full anonymous story ends with `Encounter another anonymous Moment / Ontmoet nog een anoniem moment`. Only visitor activation replaces it in the same reading surface; there is no automatic continuation. With at least two eligible stories, the next encounter does not immediately repeat the current one, and repeated encounters preserve the original indexed-story return target.
- **Confirmed:** With exactly one eligible anonymous Moment, the main invitation still opens it. The end-of-story repeat control is hidden rather than disabled, leaving only the back-to-stories control.
- **Confirmed:** With no eligible anonymous Moment, the invitation is hidden entirely. Ordinary Stories shows no disabled invitation or anonymous empty-state message.
- **Confirmed:** Anonymous Moments have no public permalink and do not change the current indexed non-anonymous URL. Refresh restores that ordinary story; anonymous bodies are excluded from sitemaps, page metadata, and search-engine indexing, without implying confidentiality against copying or screenshots.
- **Confirmed:** Anonymous attribution is exactly `Anonymous` on English pages and `Anoniem` on Dutch pages. It does not expose a name, initials, customer-chosen pseudonym, city, age, or purchase date.
- **Confirmed:** Every anonymous story uses the shared heading `A Rubiae Moment / Een Rubiae Moment`; no customer-specific or story-specific title is displayed.
- **Confirmed:** Anonymous text receives a Rubiae identifier and indirect-recognition review followed by customer approval of the final redacted original-language story and page preview. Only minimum meaning-preserving deletion or generalisation is allowed; if foreseeable recognition risk cannot be reduced without materially changing the story, it is not published anonymously.
- **Confirmed:** Every anonymous image is reviewed and stripped of EXIF/IPTC/XMP/GPS, embedded previews, and its original public filename. Only meaning-preserving crops or masks are allowed with customer approval; an image that cannot be made safe is omitted in favour of the intentionally empty plinth, without a generic or generated substitute.
- **Confirmed:** Anonymous mode never exposes a public Stone association: no Stone card, image, name, number, gemstone type, or link. An internal relationship may remain, but public association requires separate approval to publish the Moment as non-anonymous and a valid story-to-Stone association permission.
- **Open:** Final desktop visual composition, random-entry plus back/repeat-control appearance, the non-anonymous authorised Stone archive card, implementation, and interaction tests remain under `GEM-009`.

## 2026-07-31

- **Project and canonical root:** Rubiae，`/Users/jiekewei/Documents/3 - Projects/6 - 红宝石`。
- **Branch / revision:** `codex/restore-original-hero-card` at `87b3d06`; this P0 remains uncommitted pending user visual review.
- **Completed:** Implemented Stone Detail P0 in the local React preview: responsive 65/35 desktop hero, mobile identity → photo → facts order, two fact groups, three principal photographic moments, explicit preview/evidence boundaries, compact expandable millimetre/report/video evidence, EN/NL content, and one final enquiry action. No price or transaction control was added.
- **TDD and verification:** The new Stone Detail contract first failed two assertions for the missing hero/evidence structure and preview notice, then passed after implementation. Fresh verification passed: `npm test` 13/13, `npm run build`, `npm run test:sites` 4/4, and `git diff --check`.
- **Visual QA:** Reviewed the approved source beside the implementation at 1440 × 900 and 390 × 844, including closed/open evidence, mobile menu, EN/NL switching, direct-route load and horizontal overflow. No browser console errors were observed. Evidence is recorded in `preview-prototype/design-qa.md` and four fixed-viewport screenshots under `preview-prototype/qa/`.
- **Active-work records:** `GEM-005` remains open. Stone Detail's responsive shared skeleton moved to completed scope; per-stone verified facts, real photography, measurements/reports/video, individual art direction, Shopify binding and public hosting remain open. `Stones` implementation remains the next website priority.
- **Open IDs:** `GEM-001`, `GEM-002`, `GEM-003`, `GEM-004`, `GEM-005`, `GEM-006`, `GEM-007`, `GEM-008`, `GEM-009`, `GEM-010`.
- **Exact next action:** User visually reviews the local Stone Detail P0. If approved, selectively commit this scope; then start P1 `Stones` with an expected-red acceptance test before production changes.

### Stone Detail navigation contrast handover

- **Project and canonical root:** Rubiae，`/Users/jiekewei/Documents/3 - Projects/6 - 红宝石`。
- **Branch / revision:** `codex/restore-original-hero-card` at `87b3d06`; no commit was created for the Stone Detail P0 or this navigation refinement.
- **Work status:** Stone Detail P0 remains implemented and uncommitted. The user confirmed the visual direction for the next refinement: the desktop header spans the full viewport; the left `Rubiae` wordmark is paper white over the dark photograph; the right navigation and language control are dark ink over the paper-white fact rail; mobile remains dark ink on paper white. The short written specification is `docs/superpowers/specs/2026-07-31-stone-detail-navigation-contrast-design.md`. Navigation implementation, tests, and new visual QA are intentionally unstarted.
- **Tests and verification:** No navigation-refinement test has been written or run, so there is no expected-red test for this subtask. The existing P0 was last freshly verified before the refinement request with `npm test` 13/13, `npm run build`, `npm run test:sites` 4/4, and `git diff --check`. This handover inspection reran `git diff --check` successfully; it did not rerun product tests or browser QA.
- **Uncommitted state:** Inspected with `git status --short`. Modified tracked files are `CONTEXT.md`, `docs/brand/rubiae-web-visual-spec.md`, `docs/project-todos.md`, `docs/unimplemented-features.md`, `docs/website/rubiae-preview-site-content.md`, `handoff.md`, `preview-prototype/design-qa.md`, `preview-prototype/src/App.jsx`, `preview-prototype/src/content.js`, `preview-prototype/src/pages/StoneDetailPage.jsx`, and `preview-prototype/src/styles.css`. Untracked task material includes the Stone Detail approved reference, Stone Detail/Stories specs and plans, `preview-prototype/tests/stone-detail.test.mjs`, and `preview-prototype/qa/`; unrelated `.DS_Store` and `.superpowers/` remain untouched. Nothing is staged.
- **Blockers and decisions:** Design direction is confirmed. The written specification itself has not received the requested explicit `规格通过` review, so implementation remains gated. Do not use `mix-blend-mode`, a navigation background bar, overlay, gradient, haze, or shadow treatment; use deterministic route-scoped colours with the existing `site-header--stone` class. Preserve the 65/35 hero geometry and all non-navigation behaviour.
- **Exact next action:** In the new window, open `docs/superpowers/specs/2026-07-31-stone-detail-navigation-contrast-design.md`. After the user confirms `规格通过`, read `docs/project-todos.md`, `docs/unimplemented-features.md`, and this 2026-07-31 handoff; then add a `GEM-005` failing test proving a full-width Stone header with white wordmark/dark desktop navigation before changing `preview-prototype/src/styles.css`.

### Stone Detail navigation contrast implementation

- **Completed:** After explicit `规格通过`, added a `GEM-005` boundary test, observed it fail against the former `width: 65%`, then changed only route-scoped Stone header CSS. Desktop Header now spans the full viewport, with a paper-white wordmark over photography and dark navigation/language control over the fact rail. Mobile remains a paper-white in-flow header with dark wordmark/menu control and a dark opened menu with paper-white text. The 65/35 Hero and all non-navigation behaviour remain unchanged.
- **Verification:** Baseline `npm test` passed `13/13`. RED failed for the missing `width: 100%` split-contrast contract. GREEN passed `3/3` targeted and `14/14` full. Fresh `npm run build` and `npm run test:sites` (`4/4`) passed. Browser QA at `1440 × 900` and `390 × 844` confirmed `1440 px`/`390 px` Header widths, desktop `936 px / 504 px` Hero columns, working EN/NL and mobile menu, no horizontal overflow and no console errors. Evidence: `preview-prototype/qa/stone-detail-navigation-desktop-1440x900.png` and `preview-prototype/qa/stone-detail-navigation-mobile-menu-390x844.png`.
- **Active-work records:** `GEM-005` remains open for `Stones`, verified per-stone inventory evidence, Shopify binding and public hosting. Its Stone Detail P0 completed scope now includes this navigation refinement; no feature ID was added or removed.
- **Commit status:** Stone Detail P0 and this refinement remain uncommitted pending user visual review. Nothing is staged.
- **Exact next action:** User visually reviews the navigation refinement. If approved, selectively commit the complete Stone Detail P0 scope; then start P1 `Stones` with an expected-red acceptance test before production changes.

## 2026-08-01

### Mobile navigation paper-sheet refinement

- **Completed:** After the user approved the Figma audit direction, replaced the shared mobile navigation's inset dark panel with one flush paper-white sheet. Primary links use dark ink; `Close` is unboxed; EN/NL is smaller and separated by a fine rule. Routes, copy, imagery, Stone facts and desktop navigation were not changed.
- **TDD:** Baseline `npm test` passed `14/14`. Added a `GEM-005` boundary test and observed the expected RED failure against the boxed toggle/inset dark panel. After the CSS change, targeted Stone tests passed `4/4`; full `npm test` passed `15/15`.
- **Regression verification:** `npm run build`, `npm run test:sites` (`4/4`) and `git diff --check` passed. The local preview is running at port `4173`.
- **Visual QA:** The source Figma audit board is available, but this task exposes no callable in-app browser screenshot/inspection control, so the internal same-state capture remains blocked in `preview-prototype/design-qa.md`. After the preview server was restored, the user inspected the live local result and confirmed `好，没问题`; stakeholder visual acceptance is complete.
- **Active-work records:** `GEM-005` remains open for `Stones`, verified inventory evidence, Shopify binding and public hosting. The paper-sheet menu is implemented and user-approved. No feature ID was added or removed.
- **Commit status:** User approved the rendered Stone Detail P0 and mobile paper-sheet menu for one selective local commit; unrelated dirty work remains excluded.
- **Exact next action:** Selectively commit the approved Stone Detail P0 scope, then start P1 `Stones` with an expected-red acceptance test before production changes.
