<section class="home-section home-closing" aria-labelledby="ClosingTitle">
  <div class="home-closing__ruby" aria-hidden="true"></div>
  <div>
    <h2 id="ClosingTitle">{{ 'home.closing.heading' | t }}</h2>
    <a class="home-button" href="{{ routes.root_url }}pages/stones">{{ 'home.closing.cta' | t }}</a>
  </div>
</section>

{% schema %}
{ "name": "Home closing", "settings": [] }
{% endschema %}
